{
  "name": "auto_youtube_shorts_scroller",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && rm -f auto-youtube-shorts-scroller.xpi && cd extension && zip -r ../auto-youtube-shorts-scroller.xpi . -x '*.DS_Store'",
    "sign": "npm run build && set -a && . ./.env && set +a && web-ext sign --source-dir=extension --artifacts-dir=web-ext-artifacts --channel=unlisted"
  },
  "dependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "web-ext": "^10.2.0"
  }
}
